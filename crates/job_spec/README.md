# Graphile Worker Job Spec

This crate provides job specification types for [Graphile Worker](https://docs.rs/graphile_worker), allowing you to configure job options such as queue name, run time, max attempts, priority, and deduplication keys.
